{% extends 'base.html.twig' %}

{% block title %}
	Page de
	{{product.name}}
{% endblock %}

{% block body %}
	<h1>{{product.name}}
		(
		{{product.price / 100 }}
		€)</h1>


	<div class="row">
		<div class="col-4">
			<img src=" {{product.mainPicture}} " class="img-fluid" alt="image de {{product.name}}">
		</div>
        <div class="col">
        <span class="badge bg-primary">
        {{product.category.name}}
        </span>
        <p> {{product.shortDescription}} </p>
        <a href="" class="btn btn-success btn-lg">
        <i class="fas fa-shopping-cart"></i>
        ajouter au panier</a>
        </div>
	</div>


{% endblock %}
